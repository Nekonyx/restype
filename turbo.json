{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["/packages/*/lib/", "/packages/*/bin/"],
      "outputMode": "new-only"
    }
  }
}
